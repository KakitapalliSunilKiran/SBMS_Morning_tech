spring:
  application:
    name: SpringCloudGateway

  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: api-1
              uri: lb://SpringCloudGreetApi
              predicates:
                - Path=/greet/**

server:
  port: 7777
